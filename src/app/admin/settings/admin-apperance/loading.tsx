import { PageLoader } from '@/components/common/page-loader';

export default function AdminAppearanceLoading() {
    return <PageLoader open={true} text="Loading Dashboard Appearance Settings..." />;
}
