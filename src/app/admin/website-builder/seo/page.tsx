import { SeoContent } from "../_components/seo-content";

export const metadata = {
    title: "SEO Settings | Website Builder",
    description: "Configure search engine meta titles, descriptions, canonical URLs, and structured data",
};

export default function SeoPage() {
    return <SeoContent />;
}
