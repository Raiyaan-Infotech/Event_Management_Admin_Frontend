import { NavMenuContent } from "../_components/nav-menu-content";

export const metadata = {
    title: "Navigation Menu | Website Builder",
    description: "Manage header menu links and navigation",
};

export default function NavMenuPage() {
    return <NavMenuContent />;
}
