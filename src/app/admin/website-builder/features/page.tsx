import { FeaturesBuilderContent } from '../_components/features-content';

export const metadata = {
    title: 'Features Builder | Website Builder',
    description: 'Manage features section for website builder',
};

export default function FeaturesPage() {
    return <FeaturesBuilderContent />;
}
