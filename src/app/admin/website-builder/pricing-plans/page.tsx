import { PricingPlansBuilderContent } from '../_components/pricing-plans-content';

export const metadata = {
    title: 'Pricing Plans Website Builder | Event Admin',
    description: 'Configure pricing plans, custom CTA button text, feature limits, and comparison matrix for the public website.',
};

export default function PricingPlansBuilderPage() {
    return <PricingPlansBuilderContent />;
}
