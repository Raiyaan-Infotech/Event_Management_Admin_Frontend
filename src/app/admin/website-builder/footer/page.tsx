import { FooterContent } from "../_components/footer-content";

export const metadata = {
    title: "Footer Settings | Website Builder",
    description: "Configure website footer description, quick links, newsletter block, and contact details",
};

export default function FooterPage() {
    return <FooterContent />;
}
