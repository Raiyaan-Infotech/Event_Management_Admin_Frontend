import { WebsiteBuilderTranslationsContent } from './_components/website-builder-translations-content';

export const metadata = {
    title: 'Translations | Website Builder',
    description: 'Manage translations for every text field saved across Website Builder sections',
};

export default function WebsiteBuilderTranslationsPage() {
    return <WebsiteBuilderTranslationsContent />;
}
