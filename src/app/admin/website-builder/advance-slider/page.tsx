import { AdvanceSliderContent } from "../_components/advance-slider-content";

export const metadata = {
    title: "Advance Slider | Website Builder",
    description: "Manage advance interactive slider items, overlay opacity, brightness, blur, and custom button text colors",
};

export default function AdvanceSliderPage() {
    return <AdvanceSliderContent />;
}
