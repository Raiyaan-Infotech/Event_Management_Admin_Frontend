import { FaqFormContent } from '../../_components/faq-form-content';

export const metadata = {
    title: 'Add New FAQ | Website Builder Admin',
};

export default function CreateWebsiteFaqPage() {
    return <FaqFormContent />;
}
