import { FaqsListContent } from '../_components/faqs-list-content';

export const metadata = {
    title: 'FAQs | Website Builder Admin',
};

export default function WebsiteFaqsPage() {
    return <FaqsListContent />;
}
