import { ThemeColorContent } from "../_components/theme-color-content";

export const metadata = {
    title: "Theme Color | Website Builder",
    description: "Configure website 4-color theme palettes and custom hex values",
};

export default function ThemeColorPage() {
    return <ThemeColorContent />;
}
