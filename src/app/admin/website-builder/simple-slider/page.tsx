import { SimpleSliderContent } from "../_components/simple-slider-content";

export const metadata = {
    title: "Simple Slider | Website Builder",
    description: "Manage simple image slider items, titles, descriptions, and button links",
};

export default function SimpleSliderPage() {
    return <SimpleSliderContent />;
}
