import { ComingSoon } from '../_components/coming-soon';

export default function SendNotificationPage() {
    return (
        <ComingSoon
            title="Send Notification"
            description="Compose and send an ad-hoc notification using a template or a one-off message. Not built yet."
        />
    );
}
